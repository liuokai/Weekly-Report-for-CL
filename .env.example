# Server Configuration
PORT=3001
DB_HOST=127.0.0.1
DB_PORT=9030
DB_USER=root
DB_PASSWORD=
DB_NAME=changle_ops

# Dify API Configuration (Frontend)
# Note: In Vite/Webpack, variables starting with REACT_APP_ or VITE_ are exposed to the browser.
# Since this project uses Webpack, ensure dotenv-webpack is configured or variables are passed.

# Turnover Overview Analysis Workflow
REACT_APP_DIFY_TURNOVER_KEY=app-xxx-your-api-key-here
